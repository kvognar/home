nav.navbar role='navigation'
  .container-fluid
    .navbar-header
      = link_to 'Good morning', days_url, class: 'navbar-brand'
    ul.nav.navbar-nav
      li = link_to 'People', people_url
      li.dropdown
        a href='#' class='dropdown-toggle' data-toggle='dropdown' Categories
        ul.dropdown-menu role='menu'
          - Tag.categories.each do |category|
            li = link_to category.name, tag_url(category)
      li
        = link_to 'Susurrus', susurrus_url
      li
        = link_to 'Day by day', day_by_day_url, data: { confirm: 'This page will be slow! I recommend a wifi connection.' }
      - if signed_in?
        li = link_to '☂', new_day_url
